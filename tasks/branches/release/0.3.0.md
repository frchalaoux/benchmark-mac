# Objective

Finaliser et publier la version stable `v0.3.0` sans élargir le périmètre
fonctionnel validé par les préversions `v0.3.0.dev0` à `v0.3.0.dev2`.

# Current Status

- Version publiée et close-out terminé.
- Branche créée depuis `main` au commit `8dc7a53`.
- Périmètre fonctionnel figé.
- Inclus : quatre benchmarks GPU WebGPU, sélection d'adaptateur, contrôle
  préalable, corrections d'installation Windows et documentation associée.
- Exclu : correction CLI multi-machine du commit `6d4c5dd`, réservée à
  `0.3.1`.
- Version du paquet, des installateurs et de la documentation fixée à `0.3.0`.
- Compatibilité officielle limitée à `0.3.0.dev1`, `0.3.0.dev2` et `0.3.0`;
  `0.3.0.dev0` reste refusée.
- Le fichier utilisateur racine `comparaison.html` est explicitement exclu des
  archives de distribution sans être supprimé du poste de travail.
- Branche `release/0.3.0` et tag annoté `v0.3.0` publiés sur `3d50177`.
- Pull request nº 3 fusionnée dans `main` au commit `14cd225`.
- GitHub Release stable publiée et marquée `Latest`.

# Next Concrete Action

Reprendre la branche `fix/multi-machine-cli-comparison`, intégrer le nouveau
`main` stable et préparer la série `0.3.1`.

# Validation Snapshot

- `uv run ruff check .` réussi.
- `uv run ruff format --check .` réussi.
- `uv run pytest` : 49 tests réussis sur la préparation stable.
- Source et wheel `0.3.0` construites avec succès.
- Wheel installée isolément : `benchmark-mac --version` affiche `0.3.0` et la
  commande `list` fonctionne.
- Archive inspectée : versions et installateurs alignés sur `v0.3.0`, rapport
  utilisateur racine exclu.
- Campagne complète Windows 11 réussie avec 16 benchmarks et état initial
  déclaré convenable.
- Campagne macOS Intel `quick` complète réussie avec 16 benchmarks, sans swap et
  avec état initial convenable; les scores GPU sont toutefois très dispersés.
- Installation Linux amd64 validée dans Docker, y compris la reprise depuis
  `uv 0.9.30` vers `uv 0.12.19` et CPython 3.14.4.
- Les 12 benchmarks Linux non-GPU réussissent; `llvmpipe` est correctement
  refusé pour les quatre benchmarks GPU. Aucun GPU Linux matériel n'était
  transmis au conteneur.

# Key Files

- `pyproject.toml`
- `src/benchmark_mac/__init__.py`
- `install.sh`
- `install.ps1`
- `README.md`
- `docs/versions.md`
- `handoff.md`

# Watchouts

- Aucun ajout fonctionnel après le gel du périmètre.
- Ne pas intégrer `6d4c5dd` dans `v0.3.0`.
- Ne pas élargir le groupe de compatibilité à `0.3.0.dev0` ou `0.2.x`.
- Ne jamais déplacer ou recréer le tag publié `v0.3.0`.
- Ne rien publier à distance sans confirmation explicite actuelle.
- Les rapports HTML générés doivent rester dans `data/results/`; la règle
  `.gitignore` dédiée empêche leur publication sur GitHub.
