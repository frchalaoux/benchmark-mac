# Objective

Finaliser et publier la version stable `v0.3.0` sans élargir le périmètre
fonctionnel validé par les préversions `v0.3.0.dev0` à `v0.3.0.dev2`.

# Current Status

- Branche créée depuis `main` au commit `8dc7a53`.
- Périmètre fonctionnel figé.
- Inclus : quatre benchmarks GPU WebGPU, sélection d'adaptateur, contrôle
  préalable, corrections d'installation Windows et documentation associée.
- Exclu : correction CLI multi-machine du commit `6d4c5dd`, réservée à
  `0.3.1`.
- Version du paquet encore fixée à `0.3.0.dev2`.

# Next Concrete Action

Compléter la validation réelle sur les plateformes cibles, puis préparer les
métadonnées, installateurs et documents de la stable `0.3.0`.

# Validation Snapshot

- `uv run ruff check .` réussi.
- `uv run ruff format --check .` réussi.
- `uv run pytest` : 48 tests réussis sur la branche de correction; le périmètre
  figé de `main` en comptait 47 avant l'exclusion du nouveau test CLI.
- Construction source et wheel `0.3.0.dev2` réussie.
- Campagne complète Windows 11 réussie avec 16 benchmarks et état initial
  déclaré convenable.
- Campagnes macOS disponibles mais à refaire au repos pour la validation finale.
- Validation Linux encore absente.

# Key Files

- `pyproject.toml`
- `src/benchmark_mac/__init__.py`
- `install.sh`
- `install.ps1`
- `README.md`
- `docs/versions.md`
- `handoff.md`

# Watchouts

- Aucun ajout fonctionnel après le gel du périmètre.
- Ne pas intégrer `6d4c5dd` dans `v0.3.0`.
- Décider explicitement si les rapports `0.3.0.dev1`, `0.3.0.dev2` et `0.3.0`
  doivent être comparables avant de figer les règles de compatibilité.
- Ne rien publier à distance sans confirmation explicite actuelle.
- Préserver la modification locale de `.gitignore` et le fichier non suivi
  `comparaison.html`, qui sont hors périmètre.
