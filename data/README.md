# Données locales

Les commandes enregistrent leurs rapports JSON dans `data/results/`. Ces
résultats dépendent de la machine et ne sont pas versionnés. Le répertoire est
créé automatiquement si nécessaire.
